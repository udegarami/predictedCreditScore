Shap line values

GIT 
X    Set Up GIT
X    Set up GitHub 
X    Push First Folder

Dataset Preparation
X    Exploratory Analysis
X    Data Drift Analysis (use Evidently) as HTML File
X    Feature Engineering (undersampling, and oversampling smooth or class weights )
        trouver 10 clients plus proches avec KNN du client 

Back End
X    Implement Prediction Algorithm 
    Optimize Prediction (ML Flow, Calculate Error with Area under curve)
        Outils Open Source pour élaborer une plateforme MLOps
            MLFlow pour la gestion “d’expériences” et leur tracking lors de la phase d’entraînement des modèles, ainsi que la visualisation desrésultats avec MLFlow UI, pour le partager avec Chris
            MLFlow pour le stockage centralisé des modèles dans un “model registry” et le serving
        X    Git, logiciel de version de code, pour suivre les modifications ducode final de l’API de prédiction de tags à déployer
        X    Github pour stocker et partager sur le cloud le code de l’API, alimenté par un “push” Git et ainsi assurer une intégration continue
            Github Actions pour le déploiement continu et automatisé du code de l’API sur le cloud
            (optionnel)Pytest (ou Unittest) pour concevoir les tests unitaires et les exécuter de manière automatisée lors du build réalisé par GithubActions
    Json Output for API jsonify dict
    Organise code in functions
Make the back end run in the cloud
    Host on Heroku ou AzureWebApps
    Implement API (Flask or fastAPI)
    Readme file, package list

Front End
    Draft UI
    Code UI (Streamlit)
    Host UI
    Run All together
    Readme file, package list

Note méthodologique
    La méthodologie d'entraînement du modèle (2 pages maximum)
    Le traitement du déséquilibre des classes (1 page maximum)
    La fonction coût métier, l'algorithme d'optimisation et la métrique d'évaluation (1 page maximum)
    Un tableau de synthèse des résultats (1 page maximum)
    L’interprétabilité globale et locale du modèle (1 page maximum)
    Les limites et les améliorations possibles (1 page maximum)
    L’analyse du Data Drift (1 page maximum)
    Un support de présentation pour la soutenance, détaillant le travail réalisé (Powerpoint ou équivalent, 30slides maximum).
    Des copies écran des commits, du dossier Github (+ lien vers ce dossier) et de l’exécution des tests unitaires, qui sont les preuves qu’un pipeline de déploiement continu a permis de déployer l’API, doivent être formalisés dans ce support de présentation.
